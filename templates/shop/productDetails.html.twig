{% extends 'base.html.twig' %}

{% block title %}{{ product.name }}{% endblock %}

{% block body %}
    <div class="container">
        <div class="row mt-3 mb-4">
            <div class="col-md-4">
                <img src="https://via.placeholder.com/600x400" alt="{{ product.name }}" class="img-fluid">
            </div>
            <div class="col-md-8">
                <h1 class="mt-4 mt-md-0" style="color: #fff;">{{ product.name }}</h1>
                <h2 style="color: #fff;">{{ product.price }} €</h2>
                <hr>
                <b style="color: #fff;">Description: </b><p style="color: #fff;">{{ product.description }}</p>
                <a href="{{ path('cart.add', {id: product.id}) }}" class="btn btn-warning">Add To Cart</a>
            </div>
        </div>
    </div>
{% endblock %}
